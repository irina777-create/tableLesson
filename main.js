// Вивести таблицю 10 × 10, заповнену числами від 1 до 100 (таблиця створюється динамічно)

let arr=[];
for(let i = 1; i <= 100; i++) f
arr.push(i);
arr.sort (function(){ return 0.5 - Math.random()});
console.log('Массив с случайным порядком элементов: ', arr);